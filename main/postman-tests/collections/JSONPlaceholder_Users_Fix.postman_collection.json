{
  "info": {
    "name": "JSONPlaceholder - Users CRUD (Fix)",
    "_postman_id": "22222222-3333-4444-5555-666666666666",
    "description": "Coleção de testes de API para JSONPlaceholder (exemplo corrigido).",
    "schema": "https://schema.getpostman.com/json/collection/v2.1.0/collection.json"
  },
  "item": [
    {
      "name": "Listar usuários (GET /users)",
      "request": {
        "method": "GET",
        "header": [],
        "url": {
          "raw": "{{baseUrl}}/users",
          "host": [
            "{{baseUrl}}"
          ],
          "path": [
            "users"
          ]
        }
      },
      "event": [
        {
          "listen": "test",
          "script": {
            "exec": [
              "pm.test('status 200', function () { pm.response.to.have.status(200); });",
              "const data = pm.response.json();",
              "pm.test('retorna array', function () { pm.expect(Array.isArray(data)).to.be.true; });"
            ],
            "type": "text/javascript"
          }
        }
      ]
    }
  ]
}
